const Comment = require('../Banco/Comment')
const Logado = require("./Logado");

const Comentar = async (body, cod_usr) => {
    let cod_plt = body.cod_plt;
    let descricao = body.descricao;

    
    
}

module.exports = Comentar;